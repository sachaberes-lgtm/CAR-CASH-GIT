# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/

# --- Node.js ---
node_modules/
npm-debug.log*

# --- Système / OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Synchro cloud parasites ---
*.tmp
.tmp.*
# --- Sauvegardes horodatees de train.html (900 Ko piece : elles restent en local) ---
ml/train/train.backup-*.html

# --- Demos et cerveaux entraines : des DONNEES, pas du code (elles se refont) ---
# --- Donnees d entrainement ---
# ⚠ LES DEMOS SONT VERSIONNEES, ET C EST VOLONTAIRE.
# Le 2026-09-08, deux sessions de jeu ont ete detruites par un menage de fichiers non suivis :
# elles etaient gitignorees, donc invisibles pour git et sans sauvegarde. Ce sont les SEULES
# donnees que le projet ne sait pas regenerer — un cerveau se re-entraine, une piste se
# reconstruit depuis sa graine, une partie jouee ne revient jamais. Elles restent donc dans le
# depot (400-800 Ko piece, on peut se le permettre).
ml/train/results/cloned-brain.json
ml/train/results/archive/
ml/train/results/archive-*/

# --- Archive locale : vieilles versions et experiences terminees ---
_archive/

# --- Pistes generees : donnees, pas du code (track.js les refait) ---
ml/sim/track-*.json
